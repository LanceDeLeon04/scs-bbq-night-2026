# Supabase project settings (Project Settings → API in the Supabase dashboard)
# Use the Project URL and the "anon public" key — NEVER the database password
# or service_role key here, since this app ships to the browser.
VITE_SUPABASE_URL=https://syhibugrxegajtyqdroy.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# Admin login for the /admin dashboard (client-side gate, see README security note)
VITE_ADMIN_USERNAME=lance.deleon
VITE_ADMIN_PASSWORD=SCS2026-2025
